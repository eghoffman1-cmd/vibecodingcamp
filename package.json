{
  "name": "be-anything",
  "version": "1.0.0",
  "description": "Be Anything - city life simulation game",
  "main": "index.js",
  "scripts": {
    "build": "cp index.html www/index.html",
    "cap:add": "npx cap add ios",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
